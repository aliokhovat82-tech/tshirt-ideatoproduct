export function Footer() {
  return (
    <footer className="border-t border-black/[.08] px-6 py-8 text-center text-sm text-zinc-600 dark:border-white/[.1] dark:text-zinc-400">
      <p>
        © {new Date().getFullYear()} From Idea To Apparel. Turn your idea into a
        real, printable design.
      </p>
    </footer>
  );
}
